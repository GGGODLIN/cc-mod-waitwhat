{
  "description": "wait what: buttons above the prompt that retell recent turns through $.model.complete and draw the retell in the band. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1.",
  "modules": ["./register.tsx"]
}
